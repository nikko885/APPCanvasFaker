# 更新日志

本项目所有显著变更都记录在本文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（迭代期带 `-dev` 后缀）。

## [Unreleased]

### 变更

- 配置统计通道改官方 RemotePreferences 路线（`acf_config` 组，LSPosed 数据库中转）：
  自建 ContentProvider 桥（`StatsProvider`）退役，Manifest xposed 遗留元数据与
  `xposed_scope` 数组同步删除。根因：targetSdk≥30 且无包可见性声明的目标应用
  调不通自建 Provider，Hook 装不上。
- 主页哈希卡新增基线脚注（模块自身不可被 Hook，恒为本机未污染基准）与远端通道探针。

### 修复

- 作用域内目标应用（scanner 等）开启规则后无注入日志、无伪装效果的问题；
  未启用包仅一条终态日志，不再 25 次重试刷屏。

## [0.8.4-dev] - 2026-09-05

### 新增

- 文本指纹伪装更严密：此前个别应用可能通过特殊的字体测量接口绕过文本伪装，
  现已补齐；如个别应用排版异常，可在"实验性功能"中关闭文本伪装。
- 屏幕拷贝防绕过：应用通过系统截图接口获取画面后延迟读取，同样会被伪装；
  默认开启，如应用内截图分享异常，可在"实验性功能"中关闭。

## [0.8.3-dev] - 2026-08-30

### 变更

- **应用包名变更为 `dev.neekolor.appcanvasfaker`**（开发者更名 Neekolor）。
  这是破坏性变更：安装后为全新应用，需在 LSPosed 中重新启用本模块并勾选作用域；
  原应用内的 Hook / SSAID 配置不会自动迁移，确认新模块生效后可卸载旧应用。
- 本更新日志改用 Keep a Changelog 标准格式。

### 修复

- Material 皮肤主页：状态卡恢复为与 KernelSU 一致的全宽样式，
  "已 Hook 应用 / Hook 次数"两张统计卡移至状态卡下方并排展示。
- Material 皮肤 SSAID 管理页：条目较多时列表无法滚动、内容顶出屏幕。
- Material / Miuix 皮肤关于页：版权文字改为链接卡片下方的纯文本页脚，
  与 KernelSU 关于页风格一致。

## [0.8.2-dev] - 2026-08-30

### 新增

- 主题设置新增"导航栏角标"开关（默认开启）：底栏"应用列表"项的图标上显示
  已启用伪装的应用数量，横屏侧栏模式同效。

### 变更

- 关于页版权文字与链接卡片合并展示（0.8.3 已再次调整为纯文本页脚）。

## [0.8.1-dev] - 2026-08-30

### 变更

- SSAID 管理页：列表严格按系统文件原始顺序显示；条目样式对齐应用列表页
  （独立卡片、图标、等宽字体 SSAID 值、点击整行复制）；顶栏新增"显示系统应用"
  开关（默认隐藏系统应用，选择会被记住）。

## [0.8.0-dev] - 2026-08-29

### 新增

- **SSAID 管理独立页面**（设置 → SSAID 管理，入口开关位于"实验性功能"，默认关闭）：
  列出系统中全部应用的 SSAID 条目，逐条提供"随机化 / 删除"。
  两者均带二次确认，操作前会强制停止目标应用。
  这是真实的系统级写操作（需要 root），不是 Hook。

### 安全

- SSAID 写入流程加固：临时文件随机命名、写回前完整校验、通过后原子替换原文件，
  任一步失败都不会改动系统文件；全部操作串行执行，操作反馈包含缓存刷新结果。
- 统计接口不再向系统日志输出指纹明文。
- su 命令执行增加超时保护，避免界面卡死。

### 修复

- 主页与 App Profile 的指纹哈希口径与配套扫描器应用完全统一（同尺寸标准画布），
  模块内、扫描器应用、技术文档三方数值可直接比对。
- 清理无引用的遗留字符串资源；若干低危问题（临时文件回收、边界检查、
  失败反馈缺失等）。

## [0.7.1-dev] - 2026-08-29

### 修复

- 版本号显示统一为纯语义版本（如 `0.7.1-dev`）。
- App Profile 头部的应用信息即时显示，指纹与 SSAID 改为异步加载，
  不再整卡延迟等待。

## [0.7.0-dev] - 2026-08-29

### 新增

- 主页指纹信息卡新增 A2 / E1 / D1 三条基准哈希（单点读取 / 文本度量 / GPU 直读），
  与配套扫描器应用三方互比；计算失败时如实显示错误文本。
- App Profile 顶栏溢出菜单：启动 / 强制停止 / 重新启动（经 root 执行）。
- App Profile 新增"随机化 SSAID"卡片：显示该应用的真实 SSAID 值（不存在显示"空"），
  支持"删除"与"执行"随机化，均带二次确认。

### 变更

- 移除"哈希测试"页面，底栏第三项改为"待定"占位页。
- 设置页重组：单点读取链保持常开（移除独立开关，配置不变）；
  "工具"更名"实验性功能"，文本度量 / GL 直读开关移入其二级页。
- GitHub 账号由 nikko885 更名为 **Neekolor**，仓库地址与代码内链接全部更新。

## [0.6.0-dev] - 2026-08-25

### 新增

- 三条新 Hook 链，堵住像素读取之外的指纹逃逸口：
  - **单点读取**（`Bitmap.getPixel`）：与整图读取同源噪声，同一物理像素在
    整图与单点两种读法下扰动一致；
  - **文本度量族**（Paint 12 个度量重载）：施加确定性微扰，同输入恒同输出，
    不引发布局抖动；
  - **GPU 直读**（`glReadPixels`，**默认关闭**）：避免影响目标应用自身的
    GL 读回场景（游戏录像 / 推流等）。
- 上述功能均有独立开关（默认：单点读取开、文本度量开、GPU 直读关）。

### 修复

- libxposed API 依赖方式纠正：修复模块在新版 LSPosed 上完全无法加载的问题
  （"The Xposed API classes are compiled into the module's APK"）。

## [0.5.0] - 2026-08-23

### 新增

- "哈希测试"页条目新增随机化入口（直达对应应用的伪装开关）。
- 日志页新增提示横幅："启用日志记录"关闭时明示日志采集处于停止状态。

### 变更

- 模块激活判定链路重构（ConfigRepository 服务绑定判定）。
- 底栏导航图标更换（扩展图形 → 指纹图形）。

## [0.4.0] - 2026-08-23

### 新增

- "哈希测试"页：完整指纹采集与三方同口径比对（0.7.0 起移除）。

### 安全

- 统计接口鉴权收紧：校验调用方真实身份，第三方无法伪造他人统计；
  配置按需最小下发，杜绝枚举所有应用的 seed。
- 关闭云备份（`allowBackup=false`）：含 seed 的配置不再进入云备份与设备迁移。
- 日志收敛：流程日志仅调试构建输出，指纹明文不落日志。

### 修复

- Hook 安装时序加固：配置解析后台重试，消除 Provider 冷启动竞态导致的
  "开启了却没生效"。
- 输出流异常语义与宿主一致：压缩先在内存完成伪装编码再写出，任何环节失败
  都回退原始输出，绝不产生半截或重复数据。
- 并发写保护：配置与日志的"读出 → 修改 → 写回"加统一写锁；
  画布渲染 / PNG 压缩 / SHA-256 全部移出主线程（ANR 防护）。
- 主页首帧同步水合：模块状态与计数进入页面即显示，不再先显示"未激活 / 0"
  后跳变；激活状态只认服务绑定，冷启动未知窗口期显示"检测中…"。

### 变更

- 应用过滤放宽：任何应用（含系统应用）都可开启伪装。
- 模块端展示哈希统一为 16 位折叠口径，与配套扫描器应用数值可直接比对。
- "超级用户"页面更名为"应用列表"；清理上游（KernelSU）遗留命名与资源。
